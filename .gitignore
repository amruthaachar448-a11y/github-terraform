# Local state files (never commit these)
*.tfstate
*.tfstate.*

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Sensitive variable files (if you ever add secrets locally)
*.tfvars.json
